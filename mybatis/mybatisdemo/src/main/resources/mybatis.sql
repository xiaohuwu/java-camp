CREATE TABLE Customer (
                          id INT AUTO_INCREMENT PRIMARY KEY,
                          name VARCHAR(255),
                          gender VARCHAR(50),
                          telephone VARCHAR(255)
);
