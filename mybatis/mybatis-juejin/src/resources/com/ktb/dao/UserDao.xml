<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ktb.dao.UserDao">
    <resultMap id="userMap" type="com.ktb.entity.User">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="age" column="age"/>
        <result property="sex" column="sex" jdbcType="INTEGER"/>
        <result property="birthday" column="birthday"/>
        <association property="department" javaType="com.ktb.entity.Department">
            <id property="id" column="department_id"/>
            <result property="name" column="department_name"/>
        </association>
    </resultMap>

    <resultMap id="userMapLazy" type="com.ktb.entity.User">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="age" column="age"/>
        <result property="birthday" column="birthday"/>
        <association property="department"   select="com.ktb.dao.DepartmentDao.findById" column="department_id">
        </association>

    </resultMap>

    <!--注意此处的 resultMap-->
    <select id="findAll" resultMap="userMap">
        select tbl_user.*, tbl_department.name as department_name
        FROM `tbl_user` INNER JOIN `tbl_department` ON `tbl_department`.`id` = `tbl_user`.`department_id`
    </select>


    <select id="findAllByLazy" resultMap="userMapLazy">
        select *
        from tbl_user;
    </select>


    <select id="findById" resultType="com.ktb.entity.User">
        select * from tbl_user where id=#{id}
    </select>

    <select id="findUsers" resultType="com.ktb.entity.User">
        select * from tbl_user where department_id=#{id}
    </select>




    <delete id="deleteById" parameterType="string">
        delete from tbl_user where id = #{id}
    </delete>


</mapper>