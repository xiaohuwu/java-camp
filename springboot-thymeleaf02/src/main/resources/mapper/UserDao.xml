<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lou.springboot.dao.UserDao">

    <resultMap type="com.lou.springboot.model.User" id="UserResult">
        <result property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="password" column="password"/>
    </resultMap>

    <select id="findAllUsers" resultMap="UserResult">
        select *
        from tb_user;
    </select>

    <select id="getOne" resultType="com.lou.springboot.model.User">
        SELECT  `tb_user`.* FROM `tb_user` ORDER BY `tb_user`.`id` ASC LIMIT 1
    </select>

    <insert id="insertUser" parameterType="com.lou.springboot.model.User">
        INSERT INTO `tb_user` (`name`, `password`) VALUES (#{name}, #{password})
    </insert>

    <insert id="updateUser" parameterType="com.lou.springboot.model.User">
        UPDATE `tb_user` SET `name` = #{name}, `password` = #{password} WHERE `tb_user`.`id` = #{id}
    </insert>

   <delete id="deleteOne" parameterType="int">
       DELETE FROM `tb_user` WHERE `tb_user`.`id` = #{id}
   </delete>

</mapper>