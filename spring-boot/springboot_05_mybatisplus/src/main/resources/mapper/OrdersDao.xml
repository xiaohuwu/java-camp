<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springboot_05_mybatis.mapper.OrderMapper">

    <resultMap type="com.example.springboot_05_mybatis.entity.Orders" id="OrdersMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="price" column="price" jdbcType="INTEGER"/>
        <result property="remark" column="remark" jdbcType="VARCHAR"/>
        <result property="userId" column="user_id" jdbcType="INTEGER"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="version" column="version" jdbcType="INTEGER"/>
        <result property="delFlag" column="del_flag" jdbcType="INTEGER"/>
        <result property="createBy" column="create_by" jdbcType="VARCHAR"/>
        <result property="updateBy" column="update_by" jdbcType="VARCHAR"/>
        <result property="userName" column="user_name" jdbcType="VARCHAR"/>
    </resultMap>


    <resultMap id="OrderWithUser" extends="OrdersMap" type="com.example.springboot_05_mybatis.entity.Orders">
        <result property="userName" column="user_name" jdbcType="VARCHAR"/>
    </resultMap>

    <!-- 批量插入 -->
    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into orders(price, remark, user_id, update_time, create_time, version, del_flag, create_by, update_by)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.price}, #{entity.remark}, #{entity.userId}, #{entity.updateTime}, #{entity.createTime},
            #{entity.version}, #{entity.delFlag}, #{entity.createBy}, #{entity.updateBy})
        </foreach>
    </insert>


    <!-- 批量插入或按主键更新 -->
    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into orders(price, remark, user_id, update_time, create_time, version, del_flag, create_by, update_by)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.price}, #{entity.remark}, #{entity.userId}, #{entity.updateTime}, #{entity.createTime},
            #{entity.version}, #{entity.delFlag}, #{entity.createBy}, #{entity.updateBy})
        </foreach>
        on duplicate key update
        price = values(price) , remark = values(remark) , user_id = values(user_id) , update_time = values(update_time)
        , create_time = values(create_time) , version = values(version) , del_flag = values(del_flag) , create_by =
        values(create_by) , update_by = values(update_by)

    </insert>


    <select id="findAllOrders" resultMap="OrderWithUser">
        SELECT `orders`.*, user.user_name
        FROM `orders`
                 INNER JOIN `user` ON `user`.`id` = `orders`.`user_id`
    </select>


</mapper>

